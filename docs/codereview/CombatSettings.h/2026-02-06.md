# Code Review: CombatSettings.h (2026-02-06)

## 1. Objective
Public API for global combat configuration, including references to various subsystem settings (Targeting, Motion Warping, Hit Reaction).

## 5. Key Areas
*   **Encapsulation**: Centralizes global settings, allowing components to retrieve their specific configurations from a single source.

## 6. Gaps & Bugs

### GAP-1: Deprecated `AttackConfiguration` field.
*   **Priority**: Low | **Impact**: API Cleanliness
*   **Description**: The `AttackConfiguration` field is explicitly marked as `DEPRECATED` in the header, with migration logic in `CombatSettings.cpp`.
*   **Fix**: This field should be removed from `CombatSettings.h` once all existing assets are migrated and the migration code is removed.

## 7. Action Items
*   **[Cleanup]**: Remove deprecated `AttackConfiguration` field once migration is complete.

## 8. Overall Assessment
🟢 **Healthy**