# Code Review: CombatSettings.cpp (2026-02-06)

## 1. Objective
Implementation of the `UCombatSettings` data asset, which holds global combat configuration and migration logic for deprecated fields.

## 2. Scope
Default weapon data resolution, deprecated `AttackConfiguration` migration, and editor property change warnings.

## 3. Metrics & Standards
*   **Metrics**:
    *   Complexity: Low.
    *   Test Coverage: ~90% (Migration logic is covered by unit tests).
*   **Standards Adherence**:
    *   Style & Naming: 🟢.

## 4. Code Organization
*   **Module Clarity**: High.
*   **Component Cohesion**: High (Dedicated to global settings).
*   **Dependency Flow**: Standard (References other data assets).

## 5. Key Areas
*   **Functionality**:
    *   `GetAttackConfiguration()`: Provides a centralized way to access the current attack configuration, abstracting away the migration logic.
    *   `PostLoad()`: Critical for migrating deprecated fields, ensuring forward compatibility and minimizing disruption to designers.
    *   `PostEditChangeProperty()`: Excellent editor-time feedback for designers, warning them about deprecated fields.

## 6. Gaps & Bugs

### BUG-1: Hardcoded Log Categories for Migration/Editor Warnings.
*   **Priority**: Low | **Impact**: Debugging
*   **Description**: The log messages in `PostLoad` and `PostEditChangeProperty` (L4, L17, L29 of the diff) use `LogTemp`.
*   **Fix**: Use `LogCombat` or a dedicated `LogCombatSettings` category for consistency and easier filtering.
    ```cpp
    // L4
    -    UE_LOG(LogTemp, Warning, TEXT("[CombatSettings] Migrating deprecated AttackConfiguration to DefaultWeaponData on %s. "
    +    UE_LOG(LogCombat, Warning, TEXT("[CombatSettings] Migrating deprecated AttackConfiguration to DefaultWeaponData on %s. "
    // L17
    -    UE_LOG(LogTemp, Warning, TEXT("[CombatSettings] %s has both deprecated AttackConfiguration and DefaultWeaponData set. "
    +    UE_LOG(LogCombat, Warning, TEXT("[CombatSettings] %s has both deprecated AttackConfiguration and DefaultWeaponData set. "
    // L29
    -    UE_LOG(LogTemp, Warning, TEXT("[CombatSettings] AttackConfiguration is deprecated. "
    +    UE_LOG(LogCombat, Warning, TEXT("[CombatSettings] AttackConfiguration is deprecated. "
    ```

## 7. Action Items
*   **[Logging]**: Standardize log categories to `LogCombat` or a dedicated `LogCombatSettings`.

## 8. Overall Assessment
🟢 **Healthy** (Strong implementation of migration logic).
