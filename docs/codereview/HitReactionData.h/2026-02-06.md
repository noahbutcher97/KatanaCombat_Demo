# Code Review: HitReactionData.h (2026-02-06)

## 1. Objective
Public API for configuring how a character reacts to being hit, including animation montages, stun durations, and i-frames.

## 5. Key Areas
*   **Encapsulation**: Provides a data-driven approach to configuring hit reactions, separating data from logic.

## 6. Gaps & Bugs

### GAP-1: Hardcoded `StunDuration` Default.
*   **Priority**: Low | **Impact**: Maintainability
*   **Description**: `StunDuration` has a default value of `0.0f` with a comment saying "Only configure stun for specific scenarios." However, the range is `0.0f` to `5.0f`. If `0.0f` means "no stun," this is fine, but if it implies "use default from config," it should be `0.0f` to represent "no override."
*   **Fix**: Clarify the meaning of `0.0f` for `StunDuration` or consider a more explicit way to signify "no stun" vs. "default stun from config."

## 7. Action Items
*   **[Clarification]**: Clarify `StunDuration` default behavior.

## 8. Overall Assessment
🟢 **Healthy**
